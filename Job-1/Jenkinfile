pipeline{
    agent any
    stages{
        stage('Git Checkout'){
            steps{
                git branch: 'Jenkins',url: 'https://github.com/sharath050175/DevOps.git'
            }
        }
        stage('Build'){
            steps{
                echo 'Building the project...'
            }    
        }
    }
}